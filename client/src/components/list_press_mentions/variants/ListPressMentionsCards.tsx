/**
 * YAML uses variant: "cards". Filename maps to that key in sectionRegistry
 * (ListPressMentionsDefault remains the fallback when variant is omitted).
 */
export { default } from "./ListPressMentionsDefault";
