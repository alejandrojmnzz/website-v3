# Component behavioral patterns

See [docs/component-behaviors.md](../../docs/component-behaviors.md) for the full catalog.

Agents: when adding tracking, SSR schema contributors, `dynamic_entries`, or `form-settings`, declare matching `behaviors` on that component's `schema.yml`. CTA intent uses optional `tracking` on CTA objects at `cta-tracking` field-editor paths (absent ≡ `none`) — not URL sniffing.
