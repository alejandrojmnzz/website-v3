# Component behavioral patterns

See [docs/component-behaviors.md](../../docs/component-behaviors.md) for the full catalog.

Agents: when adding tracking, SSR schema contributors, `dynamic_entries`, or `form-settings`, declare matching `behaviors` on that component's `schema.yml`. CTA intent uses required `tracking` on CTA objects at `cta-tracking` field-editor paths — not URL sniffing.
